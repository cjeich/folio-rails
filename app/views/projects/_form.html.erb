<%= form_for(@project ) do |f| %>
  <% if @project.errors.any? %>
    <div id="error_explanation alert warning">
      <a href="#" class="close" data-dismiss="alert">×</a>
      <h4><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h4>

      <ul>
      <% @project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="control-group">
      <%= f.label :name, class: 'control-label' %>
    <div class="controls">
      <%= f.text_field :name %>
    </div>
</div>
  <div class="control-group">
    <%= f.label :description, class: 'control-label' %><br />
    <div class="controls">
        <%= f.text_area :description, cols: 300, rows: 4 %>
    </div>  
  </div>
  <div class="actions">
    <%= f.submit "Create", class: 'btn btn-success', style: 'width:220px' %>
  </div>
<% end %>
