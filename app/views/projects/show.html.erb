<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @project.name %>
</p>

<p>
  <b>Description:</b>
  <%= @project.description %>
</p>

<div class="btn-group">
	<%= link_to raw('<i class="icon-file"></i>  Edit'), edit_project_path(@project), class: 'btn' %>
	<%= link_to raw('<i class=" icon-circle-arrow-left"></i>  Back'), projects_path, class: 'btn' %>
</div>
